<div id="nav">
  <ul id="files" class="index_inline_list">
    <% @items.each do |button| %>
      <li class="<%= button[:selected] ? 'r2' : 'r1' %>">
      <a href="<%= button[:href] %>"><%= h(button[:label]) %></a>
      </li>
      <li>&nbsp;</li>
    <%end%>
  </ul>
</div>
