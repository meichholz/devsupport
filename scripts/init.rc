# vim: ft=sh
DIR="$1"
export LANG="C"
cd $HOME/projects
git pull

RUBY=ruby19
case "$DIR" in
  mdbrails) RUBY=ruby20 ;;
  mdbrails2) RUBY=ruby18 ;;
  *) RUBY=ruby19 ;;
esac
test -r ~/$RUBY.rc && . ~/$RUBY.rc

rake git:update

if [ -d "$DIR" ]; then
  cd "$DIR"
  rake edit
fi

